services:
  jekyll:
    build: .
    ports:
      - "127.0.0.1:4000:4000"
    volumes:
      - .:/usr/src/app
    environment:
      BUNDLE_IGNORE_CONFIG: "true"
    command: bundle exec jekyll serve --host 0.0.0.0 --config _config.yml,_config_local.yml --force_polling
